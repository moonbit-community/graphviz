# Input corpus cases intentionally excluded from strict parity manifests.
# Keep this list minimal and shrink it as alignment improves.

# === Performance issues (timeout > 15s, but complete with 60s timeout) ===
# These cases work correctly but are too slow for CI/batch testing.
# Priority: optimize layout algorithms (mincross, rank assignment, xpos).

b29   # 110 nodes, 386 edges, completes in ~35s (high in-degree nodes)
b100  # 7272 lines, large inheritance graph
b102  # 916 lines, inheritance graph  
b103  # 3385 lines, large inheritance graph
b104  # 7272 lines, large inheritance graph (duplicate of b100)
badvoro  # 1619 lines, Voronoi-related
root  # 2140 lines, uses root="..." attribute
xx    # 916 lines, inheritance graph

# === Invalid syntax in reference corpus ===
# These are bugs in the graphviz test corpus, not our implementation.

b993  # Line 1: "diGraph" should be "digraph" (case-sensitive keyword)
