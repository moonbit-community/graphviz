# Input corpus cases intentionally excluded from strict parity manifests.
# Keep this list minimal and shrink it as alignment improves.

# Note: refs/graphviz/tests/graphs/*.gv is not in INPUT_CANDIDATE_PATTERNS,
# so cases from that directory (b100, b103, b104, b993, root, etc.) cannot
# be listed here even if they timeout or fail.
